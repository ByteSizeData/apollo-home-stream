version: 2
updates:
  - package-ecosystem: github-actions        # pinned action SHAs move on their own (GitHub retires Node runtimes on a schedule)
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
